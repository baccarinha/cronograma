# 📋 Gerenciamento de Escala da Frente de Caixa - Fort Atacadista

## 🚀 Sobre o Projeto

Este projeto consiste em uma aplicação web *single-page* (SPA) desenvolvida em **HTML, CSS e JavaScript puro** para o gerenciamento eficiente da escala de trabalho dos colaboradores da Frente de Caixa da rede Fort Atacadista.

O objetivo principal é digitalizar e otimizar o processo de criação, visualização e exportação de escalas, substituindo métodos manuais por uma interface intuitiva e rica em funcionalidades. A aplicação garante a persistência dos dados localmente no navegador do usuário através do `localStorage`.

## ✨ Funcionalidades Principais

A aplicação oferece um conjunto robusto de ferramentas para a gestão operacional:

| Categoria | Funcionalidade | Descrição |
| :--- | :--- | :--- |
| **Gestão de Pessoal** | Cadastro de Funcionários | Adição, edição e remoção de colaboradores com informações como nome, setor e dia de folga. |
| **Gestão de Escalas** | Criação de Escalas | Definição de escalas de trabalho para datas específicas, com atribuição de funcionários por turno e setor. |
| **Visualização** | Calendário Interativo | Visualização das escalas criadas em um calendário, facilitando a navegação e o planejamento. |
| **Relatórios** | Exportação para PDF | Geração e download de relatórios de escala em formato PDF, prontos para impressão ou compartilhamento, utilizando `jsPDF` e `html2canvas`. |
| **Configurações** | Setores Personalizáveis | Gerenciamento de setores (Caixa, Empacotador, Tesouraria, etc.) com cores e ícones distintos para melhor organização visual. |
| **Persistência** | Armazenamento Local | Todos os dados (funcionários, setores e escalas) são salvos no navegador via `localStorage`, garantindo que as informações não sejam perdidas entre sessões. |

## 🛠️ Tecnologias Utilizadas

O projeto foi construído com foco em simplicidade, leveza e desempenho, utilizando as seguintes tecnologias:

*   **Frontend:** HTML5, CSS3 (Vanilla CSS), JavaScript (Vanilla JS)
*   **Estilização:** Font Awesome (para ícones)
*   **Geração de PDF:** `jsPDF` e `html2canvas` (bibliotecas externas para manipulação e exportação de conteúdo HTML para PDF).
*   **Persistência de Dados:** `localStorage` (API nativa do navegador).

## 💻 Instalação e Uso

Por ser uma aplicação *single-page* em HTML/CSS/JS puro, a instalação e execução são extremamente simples:

1.  **Clone o repositório** (se aplicável) ou **salve o arquivo** `pasted_content.txt` como `index.html`.
2.  **Abra o arquivo** `index.html` diretamente em qualquer navegador web moderno (Chrome, Firefox, Edge, etc.).

Não é necessário um servidor web para a execução, pois todos os recursos são carregados localmente.

## 📂 Estrutura do Projeto

O projeto é composto por um único arquivo que contém toda a estrutura (HTML), estilização (CSS) e lógica de programação (JavaScript):

```
.
└── index.html  # Contém todo o código da aplicação (HTML, CSS e JS)
```

## 📝 Licença

Este projeto está licenciado sob a Licença MIT. Veja o arquivo `LICENSE` (a ser criado) para mais detalhes.

---

*Desenvolvido por **Manus AI**.*
